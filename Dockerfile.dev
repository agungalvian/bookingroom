# Dockerfile khusus untuk development/testing
# Gunakan ini jika Dockerfile utama bermasalah

FROM node:25-alpine

WORKDIR /app

# Install dependencies
RUN apk add --no-cache libc6-compat openssl

# Copy package files
COPY package*.json ./
COPY prisma ./prisma/

# Install all dependencies
RUN npm install

# Copy app source
COPY . .

# Generate Prisma Client
RUN npx prisma generate

# Expose port
EXPOSE 3000

# Development command
CMD ["npm", "run", "dev"]
